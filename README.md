# mcp-server
# MCP Server

A simple Model Context Protocol (MCP) server built using Node.js and Express.

## Features
- JSON-RPC API
- Custom tools (weather, search, etc.)
- Easily extendable

## Run Locally
```bash
npm install
npm start
